#include <iostream>

using namespace std;

int main()
{
    //If statement to check whether the list is not empty before attempting to delete the last node
    /*
    if (!list.isEmpty()) {
        n = list.deleteFromDLLTail();
    }
    else {
        //Do not delete
    }
    */
    
    return 0;
}